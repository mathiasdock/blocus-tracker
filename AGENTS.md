# Blocus Tracker — agent entry point

Before UI work, read `DESIGN.md` (visual source of truth), `PRODUCT.md`,
recent `AI_CHANGELOG.md` entries and the relevant `.impeccable/surfaces/` brief.
Use `docs/UI.md` to locate existing implementations.

Follow DESIGN.md's coding-agent rules. Its future consolidation list is not
permission to redesign unrelated pages. Preserve working behavior and explicit
user composition constraints; distinguish documented direction from shipped UI.
